# 测试数据合并工具 — 使用说明

## 操作步骤

### 1. 选择数据源

点击 **数据源** 区域的输入框或右侧「浏览...」按钮，选择测试数据的**根文件夹**。

> 根文件夹下应包含 `EOL`、`Function test`、`Customizing` 三个子文件夹

示例路径：
```
/home/weifu/NOK/NOK测试报告
```

程序会自动递归扫描其中所有文件名包含 `Failed` 或 `Fail` 的 CSV 文件。

### 2. 选择时间范围

通过下拉选择器分别设置**开始时间**和**结束时间**：

- 年份范围：2020 ~ 2030
- 日期会根据月份和闰年自动调整（如选 2 月最多显示 29 天）
- 开始时间必须早于结束时间，否则会弹窗提示

### 3. 点击「开始合并」

程序按时间范围过滤文件，提取所有 Status=Failed 的记录，合并为一个 CSV 文件。

### 4. 查看结果

- 合并结果会在下方的表格中预览（最多显示 500 条）
- 输出文件保存在当前目录下的 **`OUT/`** 文件夹中
- 文件命名格式：`开始时间__结束时间.csv`



## 数据目录结构（参考）

```
NOK测试报告/
├── EOL/
│   └── [Failed][EOL-1][RadarTest.seq][2025-11-30][16-20-13][xxx].csv
├── Function test/
│   └── [Fail][2025-12-02 00-40-24][xxx]2.csv
└── Customizing/
    └── 2200002200021222000108160825260010520019#_20251203140309_Failed.csv
```
